@model MoneyManagerUI.RecordsTags

@{
    ViewData["Title"] = "Create";
}

<h1>Add a tag</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
            <label asp-for="RecordId" class="control-label"></label>
            <select asp-for="RecordId" class ="form-control" asp-items="ViewBag.RecordId"></select>
        </div>*@
            <input type="hidden" value="@ViewBag.RecordId" name="RecordId" />

            <div class="form-group">
                @*<label asp-for="TagId" class="control-label"></label>*@
                <select asp-for="TagId" class="form-control" asp-items="ViewBag.TagId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
